const fs = require('fs'); const { createClient } = require('@supabase/supabase-js'); const envText = fs.readFileSync('.env.local', 'utf8'); const env = Object.fromEntries(envText.split(/?
/).filter(Boolean).map(line => { const idx = line.indexOf('='); return idx === -1 ? [line, ''] : [line.slice(0, idx), line.slice(idx + 1)]; })); const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.NEXT_PUBLIC_SUPABASE_ANON_KEY); (async () => { const tables = ['fighters','events','profiles','clubs','leagues','competitions','tournaments','matches','schedules','registrations']; for (const table of tables) { try { const res = await supabase.from(table).select('*').limit(1); console.log('TABLE', table, JSON.stringify({ status: res.status, error: res.error ? res.error.message : null, dataCount: res.data?.length }, null, 2)); } catch (err) { console.log('TABLE_ERR', table, err.toString()); } } })();